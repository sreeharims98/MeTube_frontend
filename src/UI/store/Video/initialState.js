const initialState = {
  getLoading: false,
  postLoading: false,
  allVideos: [],
  video: null,
  reaction: "no_reaction",
  addVideoModal: false,
  editVideoModal: false,
};

export default initialState;
